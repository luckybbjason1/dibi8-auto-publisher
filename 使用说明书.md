# dibi8 自动发布系统 — 完整使用说明书

## 目录

1. [系统简介](#系统简介)
2. [工作原理](#工作原理)
3. [第一步：Fork 仓库](#第一步fork-仓库)
4. [第二步：申请各平台 API 密钥](#第二步申请各平台-api-密钥)
   - [Twitter/X API 申请](#1-twitterx-api-申请)
   - [Facebook API 申请](#2-facebook-api-申请)
   - [LinkedIn API 申请](#3-linkedin-api-申请)
   - [Medium API 申请](#4-medium-api-申请)
   - [Reddit API 申请](#5-reddit-api-申请)
5. [第三步：配置 GitHub Secrets](#第三步配置-github-secrets)
6. [第四步：启动与测试](#第四步启动与测试)
7. [防封号安全机制说明](#防封号安全机制说明)
8. [常见问题](#常见问题)

---

## 系统简介

本系统自动从 [dibi8.com](https://dibi8.com) 抓取文章，并定时发布到以下社交平台：

- **Twitter/X** — 发推文（带链接和话题标签）
- **Facebook** — 发到 Facebook 主页
- **LinkedIn** — 发到个人或公司主页
- **Medium** — 发布为 Medium 文章（带原文链接）
- **Reddit** — 发到指定 subreddit

**你不需要全部配置**，只需要配置你想用的平台。没有配置的平台会自动跳过。

---

## 工作原理

```
GitHub Actions 定时触发（每小时）
        ↓
  随机延迟 0-50 分钟（防检测）
        ↓
  15% 概率随机跳过本次（更像真人）
        ↓
  检查各平台 Token 是否有效
        ↓
  随机打乱平台顺序
        ↓
  对每个已配置的平台：
    → 检查是否超过每日上限
    → 检查距上次发帖是否足够久
    → 从 RSS 随机选一篇未发过的文章
    → 用该平台专属模板生成内容（每次不同）
    → 发布
    → 等待 1-5 分钟再发下一个平台
```

---

## 第一步：Fork 仓库

1. 打开 https://github.com/dibi8/new
2. 点击右上角 **Fork** 按钮
3. 选择你的 GitHub 账号
4. Fork 完成后，你的仓库地址为 `https://github.com/你的用户名/new`

> 如果你不 Fork，直接在原仓库配置也可以。

---

## 第二步：申请各平台 API 密钥

### 1. Twitter/X API 申请

**需要的密钥（4个）：**
- `TWITTER_API_KEY` — API 密钥
- `TWITTER_API_SECRET` — API 密钥密码
- `TWITTER_ACCESS_TOKEN` — 访问令牌
- `TWITTER_ACCESS_SECRET` — 访问令牌密码

**申请步骤：**

1. 打开 [Twitter 开发者平台](https://developer.twitter.com/en/portal/dashboard)
2. 用你的 Twitter 账号登录
3. 如果是第一次，需要申请开发者账号：
   - 点击 **Sign up for Free Account**（免费账号）
   - 填写你的用途说明（写"Sharing tech articles from my blog"即可）
   - 同意条款，提交申请
4. 申请通过后（通常立即通过），进入 **Dashboard**
5. 点击 **+ Create Project**：
   - Project Name：`dibi8-publisher`（随便起）
   - Use case：选 **Making a bot**
   - Project description：`Auto share articles from dibi8.com`
6. 创建 **App**：
   - App Name：`dibi8-auto-publisher`
   - 点击 **Complete**
7. 系统会显示 **API Key** 和 **API Key Secret** — **立刻复制保存！只显示一次！**
   - `API Key` = `TWITTER_API_KEY`
   - `API Key Secret` = `TWITTER_API_SECRET`
8. 设置权限为读写：
   - 进入 App Settings → **User authentication settings** → **Set up**
   - App permissions 选择 **Read and Write**
   - Type of App 选择 **Web App, Automated App or Bot**
   - Callback URL 填 `https://dibi8.com`（随便填一个）
   - Website URL 填 `https://dibi8.com`
   - 点击 **Save**
9. 生成访问令牌：
   - 回到 App 页面 → **Keys and Tokens** 标签
   - 在 **Access Token and Secret** 部分点击 **Generate**
   - 复制保存：
     - `Access Token` = `TWITTER_ACCESS_TOKEN`
     - `Access Token Secret` = `TWITTER_ACCESS_SECRET`

> ⚠️ **免费版限制**：每月 1,500 条推文（约每天 50 条），本系统每天最多发 6 条，完全够用。
>
> ⚠️ **注意**：生成的密钥只显示一次，如果丢了需要重新生成。

---

### 2. Facebook API 申请

**需要的密钥（2个）：**
- `FACEBOOK_PAGE_ID` — Facebook 主页 ID
- `FACEBOOK_ACCESS_TOKEN` — 主页访问令牌

**前提条件：**
- 你需要有一个 **Facebook 主页**（Page），不是个人账号
- 如果没有，先去 https://www.facebook.com/pages/create 创建一个

**申请步骤：**

1. 打开 [Meta 开发者平台](https://developers.facebook.com/)
2. 点击右上角 **开始** 或 **My Apps**
3. 点击 **创建应用** (Create App)：
   - 选择 **商家** (Business) 类型
   - App Name：`dibi8-publisher`
   - 点击 **创建应用**
4. 在应用面板中，找到 **添加产品**，添加 **Facebook 登录** (Facebook Login)
5. 获取临时令牌：
   - 打开 [Graph API 探索工具](https://developers.facebook.com/tools/explorer/)
   - 右上角选择你刚创建的应用
   - 点击 **Generate Access Token**（生成访问令牌）
   - 勾选权限：`pages_manage_posts`、`pages_read_engagement`
   - 点击 **生成** 并授权
6. 把短期令牌换成长期令牌：
   - 在浏览器地址栏访问以下 URL（替换花括号内容）：
   ```
   https://graph.facebook.com/v19.0/oauth/access_token?grant_type=fb_exchange_token&client_id={你的应用ID}&client_secret={你的应用密钥}&fb_exchange_token={上一步的短期令牌}
   ```
   - 应用 ID 和密钥在 **应用设置** → **基本** 里面
   - 返回的 JSON 中 `access_token` 就是长期令牌
7. 获取主页令牌和主页 ID：
   - 访问以下 URL：
   ```
   https://graph.facebook.com/v19.0/me/accounts?access_token={长期用户令牌}
   ```
   - 在返回的 JSON 中找到你的主页：
     - `id` = `FACEBOOK_PAGE_ID`
     - `access_token` = `FACEBOOK_ACCESS_TOKEN`（这个是永久的主页令牌）

> 💡 **主页令牌不会过期**，所以配置一次就好。
>
> ⚠️ 步骤较多，但配置一次后不需要再动。

---

### 3. LinkedIn API 申请

**需要的密钥（1-2个）：**
- `LINKEDIN_ACCESS_TOKEN` — OAuth 2.0 访问令牌（**必须**）
- `LINKEDIN_ORG_ID` — 公司主页 ID（可选，如果要发到公司主页）

**申请步骤：**

1. 打开 [LinkedIn 开发者平台](https://www.linkedin.com/developers/)
2. 点击 **Create App**（创建应用）：
   - App Name：`dibi8-publisher`
   - LinkedIn Page：关联你的 LinkedIn 公司主页（如果没有，先创建一个）
   - App Logo：随便上传一个图片
   - 勾选同意条款
   - 点击 **Create App**
3. 申请 API 产品权限：
   - 进入应用详情 → **Products** 标签
   - 点击申请 **Share on LinkedIn**（分享到 LinkedIn）
   - 点击申请 **Sign In with LinkedIn using OpenID Connect**
   - 等待审批（通常几分钟内自动通过）
4. 获取访问令牌：
   - 进入 **Auth** 标签，复制 **Client ID** 和 **Client Secret**
   - 在浏览器中打开以下 URL 获取授权码（替换 `{CLIENT_ID}`）：
   ```
   https://www.linkedin.com/oauth/v2/authorization?response_type=code&client_id={CLIENT_ID}&redirect_uri=https://dibi8.com&scope=openid%20profile%20w_member_social&state=random123
   ```
   - 登录并授权后，浏览器会跳转到 `https://dibi8.com?code=XXXXX&state=random123`
   - 复制 URL 中的 `code` 值
   - 用 curl 或 Postman 换取令牌：
   ```bash
   curl -X POST https://www.linkedin.com/oauth/v2/accessToken \
     -d "grant_type=authorization_code" \
     -d "code={上一步的code}" \
     -d "redirect_uri=https://dibi8.com" \
     -d "client_id={CLIENT_ID}" \
     -d "client_secret={CLIENT_SECRET}"
   ```
   - 返回的 JSON 中 `access_token` 就是 `LINKEDIN_ACCESS_TOKEN`
5. 如果要发到公司主页：
   - 打开你的公司主页 URL，如 `https://www.linkedin.com/company/12345678`
   - 最后面的数字 `12345678` 就是 `LINKEDIN_ORG_ID`
   - 还需要额外申请 `w_organization_social` 权限

> ⚠️ **重要**：LinkedIn 令牌 **60 天后过期**！你需要每 60 天重新获取一次。建议设个日历提醒。

---

### 4. Medium API 申请

**需要的密钥（1个）：**
- `MEDIUM_TOKEN` — 集成令牌

**这是最简单的平台！**

**申请步骤：**

1. 登录 [Medium](https://medium.com)
2. 点击右上角头像 → **Settings**（设置）
3. 在左侧菜单找到 **Security and apps**（安全和应用）
4. 滚动到 **Integration tokens**（集成令牌）
5. 在输入框中输入描述：`dibi8-auto-publisher`
6. 点击 **Get integration token**（获取集成令牌）
7. 复制显示的令牌 = `MEDIUM_TOKEN`

> 就这么简单！30 秒搞定。

---

### 5. Reddit API 申请

**需要的密钥（4-5个）：**
- `REDDIT_CLIENT_ID` — 应用 Client ID
- `REDDIT_CLIENT_SECRET` — 应用 Client Secret
- `REDDIT_USERNAME` — 你的 Reddit 用户名
- `REDDIT_PASSWORD` — 你的 Reddit 密码
- `REDDIT_SUBREDDITS` — 目标 subreddit 列表（可选，默认 `artificial`）

**申请步骤：**

1. 登录 [Reddit](https://www.reddit.com)
2. 打开 [应用管理页面](https://www.reddit.com/prefs/apps)
3. 滚动到页面底部，点击 **create another app...**（创建另一个应用）
4. 填写信息：
   - **name**：`dibi8-auto-publisher`
   - **App type**：选择 **script**（脚本）
   - **description**：`Auto share articles from dibi8.com`
   - **about url**：`https://dibi8.com`
   - **redirect uri**：`http://localhost:8080`
5. 点击 **create app**（创建应用）
6. 创建成功后：
   - 应用名下方的一串字符 = `REDDIT_CLIENT_ID`（大概像 `aB3cD4eF5g`）
   - **secret** 那一行 = `REDDIT_CLIENT_SECRET`
7. `REDDIT_USERNAME` 和 `REDDIT_PASSWORD` 就是你的 Reddit 登录账号密码

**配置多个 Subreddit（推荐）：**

为了避免在同一个 subreddit 发太多帖子被封，建议配置多个 subreddit 轮换：

```
REDDIT_SUBREDDITS=artificial,MachineLearning,OpenSource,selfhosted,commandline
```

系统每次会随机选一个 subreddit 发帖。

> ⚠️ **Reddit 封号风险最高**，请注意：
> - 新账号不要立即启用自动发帖，先手动发帖和评论积累 karma
> - 不要只发链接不参与社区讨论
> - 每天最多自动发 2 帖（系统已限制）
> - 不同 subreddit 有不同规则，提前看看规则

---

## 第三步：配置 GitHub Secrets

拿到各平台的 API 密钥后，把它们添加到 GitHub 仓库：

1. 打开你的仓库页面（如 `https://github.com/dibi8/new`）
2. 点击顶部的 **Settings**（设置）标签
3. 在左侧菜单找到 **Secrets and variables** → **Actions**
4. 点击 **New repository secret**（新建仓库密钥）
5. 逐一添加你要用的平台密钥：

| 名称（Name） | 值（Value） |
|---|---|
| `TWITTER_API_KEY` | 你的 Twitter API Key |
| `TWITTER_API_SECRET` | 你的 Twitter API Secret |
| `TWITTER_ACCESS_TOKEN` | 你的 Twitter Access Token |
| `TWITTER_ACCESS_SECRET` | 你的 Twitter Access Token Secret |
| `FACEBOOK_PAGE_ID` | 你的 Facebook 主页 ID |
| `FACEBOOK_ACCESS_TOKEN` | 你的 Facebook 主页令牌 |
| `LINKEDIN_ACCESS_TOKEN` | 你的 LinkedIn 访问令牌 |
| `LINKEDIN_ORG_ID` | 你的 LinkedIn 公司 ID（可选） |
| `MEDIUM_TOKEN` | 你的 Medium 集成令牌 |
| `REDDIT_CLIENT_ID` | 你的 Reddit Client ID |
| `REDDIT_CLIENT_SECRET` | 你的 Reddit Client Secret |
| `REDDIT_USERNAME` | 你的 Reddit 用户名 |
| `REDDIT_PASSWORD` | 你的 Reddit 密码 |
| `REDDIT_SUBREDDITS` | subreddit 列表，逗号分隔 |

> 💡 **只需要添加你想用的平台的密钥**，其他的不用管。

---

## 第四步：启动与测试

### 手动触发测试

1. 打开仓库的 **Actions** 标签
2. 在左侧找到 **Auto Publish dibi8 Articles**
3. 点击 **Run workflow** → 再点击绿色的 **Run workflow** 按钮
4. 等待运行完成，点击进去查看日志
5. 日志中你会看到：
   - 启动延迟时间
   - 各平台 Token 验证结果
   - 发帖结果（成功/失败/跳过）

### 自动运行

配置好 Secrets 后，系统会自动按以下时间运行：

| 时区 | 运行时间 |
|------|---------|
| 亚洲（UTC+8） | 每天 9:00 - 17:00，每小时触发 |
| 美国（UTC-5） | 每天 9:00 - 17:00，每小时触发 |

实际发帖时间因为有 0-50 分钟随机延迟 + 15% 跳过概率，所以是**完全随机的**。

### 本地测试

```bash
# 安装依赖
pip install -r requirements.txt

# 设置环境变量（禁用延迟，方便测试）
export STARTUP_JITTER_MAX=0
export INTER_PLATFORM_DELAY_MIN=0
export INTER_PLATFORM_DELAY_MAX=0
export RANDOM_SKIP_PROBABILITY=0

# 设置你要测试的平台密钥
export TWITTER_API_KEY="你的密钥"
export TWITTER_API_SECRET="你的密钥"
export TWITTER_ACCESS_TOKEN="你的令牌"
export TWITTER_ACCESS_SECRET="你的令牌"

# 运行
python run.py
```

---

## 防封号安全机制说明

本系统内置了多层防封号保护，以下是详细说明：

### 1. 发帖时间完全随机

- GitHub Actions 每小时触发一次，但触发时间不是整点（在 :17 和 :43 触发）
- 每次启动后还会随机等待 0-50 分钟
- 所以实际发帖时间在一小时内的任意时刻
- 外部完全看不出是自动化

### 2. 随机跳过

- 每次运行有 15% 的概率直接跳过不发帖
- 让发帖频率不规律，更像真人

### 3. 每个平台的发帖上限

| 平台 | 每天最多 | 最少间隔 |
|------|---------|---------|
| Twitter | 6 帖 | 2 小时 |
| Facebook | 4 帖 | 3 小时 |
| LinkedIn | 2 帖 | 6 小时 |
| Medium | 1 篇 | 24 小时 |
| Reddit | 2 帖 | 8 小时 |

### 4. 内容每次都不同

- 每个平台用不同的文案模板（Twitter 有 7 种模板，Facebook/LinkedIn 有 4 种等）
- 话题标签根据文章类别随机选择
- 开头语/hook 每次随机
- Reddit 标题前缀随机变化

### 5. 平台之间有延迟

- 发完一个平台后，等 1-5 分钟再发下一个
- 不会在同一秒同时发所有平台

### 6. 错误智能处理

- 遇到限流会自动等待并重试（指数退避）
- 遇到 Token 过期会立即停止并记录日志
- 遇到重复内容会自动跳过

---

## 常见问题

### Q: 会被封号吗？

系统已经做了很多防护，但没有任何自动化工具能 100% 保证不被封。建议：
- **循序渐进**：先只启用 1-2 个平台，运行几周后再添加
- **Reddit 要特别小心**：确保账号有足够的 karma 和正常互动
- **LinkedIn Token 要定期刷新**：每 60 天需要重新获取

### Q: 一天能发多少帖？

每个平台的每日上限见上方表格。所有平台加起来一天最多发 15 帖左右。

### Q: 如果所有文章都发过了怎么办？

系统会自动重置，从头开始循环发布。dibi8.com 目前有 257+ 篇文章，以当前频率要很久才会全部发完。

### Q: LinkedIn 令牌过期了怎么办？

重复上面 LinkedIn API 申请的第 4 步获取新令牌，然后更新 GitHub Secret 中的 `LINKEDIN_ACCESS_TOKEN`。

### Q: 如何查看发帖日志？

1. 打开 GitHub 仓库 → **Actions** 标签
2. 点击最近的运行记录
3. 展开 **Run publisher** 步骤查看详细日志

### Q: 如何暂停自动发帖？

1. 打开 GitHub 仓库 → **Actions** 标签
2. 点击左侧的 **Auto Publish dibi8 Articles**
3. 点击右上角的 **...** → **Disable workflow**

### Q: 如何修改发帖频率？

修改 `.github/workflows/auto-publish.yml` 中的环境变量：
- `RANDOM_SKIP_PROBABILITY`：增大到 0.3 表示 30% 概率跳过（发帖更少）
- 或者修改 `src/safety.py` 中的 `DAILY_CAPS` 字典

### Q: 如何添加新的 subreddit？

更新 GitHub Secret 中的 `REDDIT_SUBREDDITS`，用逗号分隔：
```
artificial,MachineLearning,OpenSource,selfhosted,commandline,ArtificialIntelligence
```

---

如有其他问题，请在 GitHub Issues 中提出。
